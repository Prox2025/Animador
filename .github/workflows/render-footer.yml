name: Remover Fundo do Vídeo

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  processar_video:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    - name: Instalar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Instalar dependências Python
      run: |
        python -m pip install --upgrade pip
        pip install Pillow

    - name: Instalar FFmpeg
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg

    - name: Criar pastas necessárias
      run: |
        mkdir -p videos
        mkdir -p saida

    - name: (Opcional) Baixar vídeo automaticamente
      run: |
        echo "🔁 Substitua este passo pelo Puppeteer ou outro método de download."

    - name: Verificar se o vídeo foi baixado corretamente
      run: |
        if [ ! -f videos/entrada.mp4 ]; then
          echo "❌ O arquivo videos/entrada.mp4 não foi encontrado!"
          exit 1
        fi

    - name: Listar arquivos disponíveis
      run: ls -lh videos/

    - name: Executar script Python para remover fundo
      run: python remover_fundo.py

    - name: Verificar arquivos de saída
      run: ls -lh saida/

    - name: Upload do vídeo com fundo removido
      uses: actions/upload-artifact@v4
      with:
        name: video_sem_fundo
        path: saida/video_sem_fundo.webm
